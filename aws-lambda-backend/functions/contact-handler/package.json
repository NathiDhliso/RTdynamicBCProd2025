{
  "name": "rtdbc-contact-handler",
  "version": "1.0.0",
  "description": "AWS Lambda function for handling contact form submissions",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "zip -r contact-handler.zip . -x \"*.zip\" \"node_modules/.cache/*\" \"*.log\"",
    "deploy": "aws lambda update-function-code --function-name rtdbc-contact-handler --zip-file fileb://contact-handler.zip"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.450.0"
  },
  "keywords": [
    "aws-lambda",
    "contact-form",
    "ses",
    "email",
    "serverless"
  ],
  "author": "RT Dynamic Business Consulting",
  "license": "ISC",
  "engines": {
    "node": ">=20.0.0"
  }
}